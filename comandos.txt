git status
git branch design
git checkout design
git add
git config user.name 'name'
git config user.email email
git pull
git push
git branch -D nome branch -- deleta branch
git push -u origin design -- cria uma branch remota design no repositório referente ao origin e cria uma ligação.
git branch -r lista branchs remotas
git branch -a lista todas as branchs
git branch -t cria referência entre uma branch local e uma branch remota
git checkout -b -- cria uma nova branch e já muda automaticamente, mas não cria link entre a nova branch local e a branch remota.
git push origin design -- atualizar os commits do repositório remoto.
git push origin :design -- este comando removev a branch remota design.
git commit -m 'mensagem'
git pull origin master
git mergetool
git mergetool -t nome_do_programa
git commit -b -m 'mensagem'
git rebase master desenvolvimento
git checkout -b testerebase

